[package]
name = "m-charts-wasm"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
smallvec = "1.15"

[lib]
crate-type = ["cdylib"]

[profile.release]
codegen-units = 1
lto = "fat"
opt-level = 3
panic = "abort"
strip = "symbols"
